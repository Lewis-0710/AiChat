name: aigroup_desktop
description: "AI聊天室 - 多AI协作对话系统"
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  provider: ^6.1.2
  path_provider: ^2.1.5
  http: ^1.3.0
  uuid: ^4.5.1
  flutter_markdown: ^0.7.7
  path: ^1.9.1
  pasteboard: ^0.5.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
