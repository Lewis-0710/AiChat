# AI聊天室项目交接文档

## 1. 项目概述

### 1.1 项目背景
AI聊天室是一个基于Flutter开发的桌面端多AI协作对话系统，旨在实现多个AI成员的协同对话。项目采用现代化的开发技术栈，具有良好的扩展性和维护性。

### 1.2 核心功能
- 多AI成员管理和协作对话
- 支持多种AI工具（Codex、Claude、OpenCode、PI、Goose、Cursor、Zcode、TraeCode、CodeBuddy）
- 本地SDK智能检测和配置
- 流式对话和实时回复
- 对话管理和历史记录

### 1.3 技术栈
- **前端框架**：Flutter 3.8.0
- **状态管理**：Provider
- **网络请求**：http
- **本地存储**：JSON文件
- **UI设计**：Material Design 3

## 2. 项目结构

### 2.1 目录结构
```
aigroup_desktop/
├── lib/                          # 源代码目录
│   ├── app.dart                  # 应用入口配置
│   ├── main.dart                 # 应用主入口
│   ├── models/                   # 数据模型
│   │   ├── member.dart           # AI成员模型
│   │   ├── conversation.dart     # 对话模型
│   │   └── message.dart          # 消息模型
│   ├── providers/                # 状态管理
│   │   ├── member_provider.dart  # 成员管理Provider
│   │   └── conversation_provider.dart  # 对话管理Provider
│   ├── services/                 # 业务服务
│   │   ├── api_client.dart      # API客户端实现
│   │   ├── storage_service.dart  # 本地存储服务
│   │   ├── sdk_detector.dart     # SDK检测服务
│   │   └── model_fetcher.dart   # 模型获取服务
│   ├── pages/                   # 页面
│   │   ├── home_page.dart       # 主页面
│   │   ├── member_management_page.dart  # 成员管理页面
│   │   └── widgets/             # 页面组件
│   ├── theme/                   # 主题样式
│   │   └── app_theme.dart       # 应用主题
│   └── utils/                   # 工具类
│       └── constants.dart       # 常量定义
├── test/                        # 测试文件
│   └── sdk_detector_test.dart   # SDK检测测试
├── pubspec.yaml                 # 项目配置
├── README.md                    # 项目说明
├── 系统设计文档.md              # 系统设计文档
└── 交接文档.md                  # 本文档
```

### 2.2 核心文件说明

#### 2.2.1 应用入口
- **main.dart**: 应用启动入口，初始化存储服务
- **app.dart**: 应用配置，包括主题、Provider注册

#### 2.2.2 数据模型
- **member.dart**: AI成员数据结构，包含工具类型、API配置等
- **conversation.dart**: 对话数据结构，包含成员列表、消息等
- **message.dart**: 消息数据结构，包含内容、图片、时间等

#### 2.2.3 状态管理
- **member_provider.dart**: 管理AI成员的增删改查和SDK检测
- **conversation_provider.dart**: 管理对话的创建、消息发送、AI协作

#### 2.2.4 业务服务
- **api_client.dart**: 实现不同AI工具的API调用
- **storage_service.dart**: 本地JSON文件存储
- **sdk_detector.dart**: 智能检测本地AI工具SDK
- **model_fetcher.dart**: 从API获取可用模型列表

## 3. 开发环境配置

### 3.1 环境要求
- Flutter 3.8.0+
- Dart 3.8.0+
- macOS 10.14+ / Windows 10+ / Ubuntu 18.04+

### 3.2 安装步骤
```bash
# 1. 克隆项目
git clone <repository-url>
cd aigroup_desktop

# 2. 安装依赖
flutter pub get

# 3. 运行项目
flutter run
```

### 3.3 开发工具推荐
- IDE: VS Code + Flutter/Dart插件
- 模拟器: iOS Simulator / Android Emulator
- 调试工具: Flutter DevTools

## 4. 核心功能模块

### 4.1 成员管理模块

#### 4.1.1 功能说明
- 添加、编辑、删除AI成员
- 支持多种AI工具选择
- SDK自动检测和手动配置
- API密钥管理

#### 4.1.2 关键代码
```dart
// 成员Provider
class MemberProvider extends ChangeNotifier {
  // 添加成员
  Future<void> addMember(Member member) async {
    final newMember = member.copyWith(id: _uuid.v4());
    _members.add(newMember);
    await _storage.saveMembers(_members);
    notifyListeners();
  }

  // SDK检测
  Future<SdkDetectionResult> detectSdk(AiTool tool) async {
    return await _sdkDetector.detect(tool);
  }
}
```

#### 4.1.3 扩展点
- 新增AI工具类型
- 自定义SDK检测规则
- API配置验证

### 4.2 对话管理模块

#### 4.2.1 功能说明
- 创建、编辑、删除对话
- 多AI成员协作
- 消息发送和接收
- 流式回复支持

#### 4.2.2 关键代码
```dart
// 对话Provider
class ConversationProvider extends ChangeNotifier {
  // 发送消息
  Future<void> sendMessage({
    required String conversationId,
    required String userMessage,
    List<String>? imageDatas,
  }) async {
    // 创建用户消息
    // 触发AI回复
    // 处理流式响应
  }

  // AI协作逻辑
  Future<void> _checkAndTriggerNextResponder({...}) async {
    // 检查并触发下一个AI回复
  }
}
```

#### 4.2.3 扩展点
- 新增消息类型
- 自定义协作策略
- 消息过滤和搜索

### 4.3 API客户端模块

#### 4.3.1 功能说明
- 统一的API调用接口
- 支持多种AI工具
- 流式响应处理
- 错误处理和重试

#### 4.3.2 关键代码
```dart
// API客户端抽象
abstract class ApiClient {
  Future<String> sendMessage({...});
  Stream<String> sendMessageStream({...});
}

// 工厂类
class ApiClientFactory {
  static ApiClient create(AccessType accessType) {
    switch (accessType) {
      case AccessType.openaiChat:
        return OpenAiChatClient();
      case AccessType.openaiResponse:
        return OpenAiResponseClient();
      case AccessType.anthropic:
        return AnthropicClient();
    }
  }
}
```

#### 4.3.3 扩展点
- 新增API接入方式
- 自定义请求处理
- 缓存机制

### 4.4 SDK检测模块

#### 4.4.1 功能说明
- 自动检测本地AI工具
- 支持多种安装路径
- 版本信息获取
- 路径验证

#### 4.4.2 关键代码
```dart
// SDK检测器
class SdkDetector {
  // 检测流程
  Future<SdkDetectionResult> detect(AiTool tool) async {
    // 1. PATH查找
    // 2. 固定路径搜索
    // 3. 目录递归搜索
    // 4. macOS应用搜索
    // 5. 主目录扫描
  }

  // 路径展开
  Future<String> _expandPath(String path, {String? npmPrefix}) async {
    // 处理~和<npm-prefix>
  }
}
```

#### 4.4.3 扩展点
- 新增检测路径
- 自定义检测策略
- 跨平台支持

## 5. 数据存储设计

### 5.1 存储结构
```
Application Support/aigroup/
├── members.json         # 成员数据
└── conversations.json   # 对话数据
```

### 5.2 数据格式

#### 5.2.1 members.json
```json
[
  {
    "id": "uuid",
    "name": "成员名称",
    "aiTool": "codex",
    "accessType": "openaiChat",
    "providerUrl": "https://api.openai.com/v1",
    "apiKey": "sk-xxx",
    "modelId": "gpt-4",
    "sdkPath": "/path/to/sdk",
    "sdkConfigured": true
  }
]
```

#### 5.2.2 conversations.json
```json
[
  {
    "id": "uuid",
    "title": "对话标题",
    "memberIds": ["id1", "id2"],
    "createdAt": "2024-01-01T00:00:00.000",
    "updatedAt": "2024-01-01T00:00:00.000",
    "isActive": true
  }
]
```

### 5.3 数据迁移
- 版本升级时的数据兼容处理
- 数据格式验证和转换
- 备份和恢复机制

## 6. UI/UX设计

### 6.1 设计原则
- Material Design 3规范
- 深色主题优先
- 响应式布局
- 无障碍访问

### 6.2 页面布局
```
┌─────────────────────────────────┐
│          标题栏                  │
├─────────────────┬─────────────────┤
│   对话列表      │   消息区域      │
│                 │                 │
│   - 对话标题    │   - 消息列表    │
│   - 创建时间    │   - 输入框      │
│                 │   - 发送按钮    │
├─────────────────┴─────────────────┤
│          状态栏                  │
└─────────────────────────────────┘
```

### 6.3 交互设计
- 拖拽上传图片
- @成员功能
- 流式回复动画
- 快捷键支持

## 7. 测试策略

### 7.1 单元测试
```bash
# 运行所有测试
flutter test

# 运行特定测试
flutter test test/sdk_detector_test.dart

# 生成覆盖率报告
flutter test --coverage
```

### 7.2 测试覆盖
- SDK检测逻辑
- API调用模拟
- 数据序列化
- UI组件交互

### 7.3 持续集成
- GitHub Actions自动化测试
- 代码质量检查
- 自动化部署

## 8. 部署和发布

### 8.1 构建流程
```bash
# 开发构建
flutter run --debug

# 发布构建
flutter build --release

# 各平台构建
flutter build apk --release
flutter build ios --release
flutter build windows --release
```

### 8.2 发布清单
- [ ] 代码审查
- [ ] 测试验证
- [ ] 文档更新
- [ ] 版本号更新
- [ ] 构建打包
- [ ] 发布渠道配置

### 8.3 版本管理
- 使用语义化版本号
- 维护CHANGELOG.md
- 分支管理策略

## 9. 常见问题和解决方案

### 9.1 开发环境问题
**问题**: Flutter版本不兼容
**解决**: 使用`flutter upgrade`更新到指定版本

**问题**: 依赖包冲突
**解决**: 检查pubspec.yaml，更新依赖版本

### 9.2 运行时问题
**问题**: SDK检测失败
**解决**: 手动配置SDK路径，检查路径权限

**问题**: API调用失败
**解决**: 检查网络连接，验证API密钥

### 9.3 性能问题
**问题**: UI卡顿
**解决**: 优化列表渲染，减少不必要的重建

**问题**: 内存占用高
**解决**: 及时释放资源，避免内存泄漏

## 10. 后续开发建议

### 10.1 功能增强
- 添加语音输入输出
- 支持文件上传和分享
- 实现消息搜索和过滤
- 添加通知功能

### 10.2 技术优化
- 添加单元测试覆盖率
- 实现数据缓存机制
- 优化网络请求性能
- 添加错误监控

### 10.3 用户体验
- 添加主题切换
- 支持自定义快捷键
- 优化移动端适配
- 添加多语言支持

## 11. 联系方式和支持

### 11.1 技术支持
- 项目负责人：[姓名]
- 邮箱：[email]
- 技术文档：[文档链接]

### 11.2 问题反馈
- GitHub Issues
- 邮件列表
- 社区论坛

### 11.3 代码贡献
- Fork项目
- 创建特性分支
- 提交Pull Request
- 参与代码审查

## 12. 附录

### 12.1 API配置示例
```yaml
# OpenAI配置
providerUrl: https://api.openai.com/v1
apiKey: sk-your-api-key
model: gpt-4

# Anthropic配置
providerUrl: https://api.anthropic.com
apiKey: your-api-key
model: claude-3-opus
```

### 12.2 SDK路径示例
```bash
# macOS应用路径
/Applications/ChatGPT.app/Contents/MacOS/ChatGPT
/Applications/Claude.app/Contents/MacOS/Claude

# npm全局包路径
~/.npm-global/bin/traecli
/opt/homebrew/bin/codebuddy

# 用户目录路径
~/.zcode/cli/zcode
~/.opencode
```

### 12.3 调试技巧
- 使用Flutter Inspector检查UI
- 使用Dart Observatory调试内存
- 使用print()输出调试信息
- 使用assert()验证假设

---

**交接完成日期**：2024年8月24日
**交接人**：[姓名]
**接收人**：[姓名]
**文档版本**：1.0.0
